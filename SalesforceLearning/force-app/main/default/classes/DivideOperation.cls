/**
 * Objective:
 * Find if given number n is divisible by 3 or not.
 * 1. Create a non static variable num to store the input
 * 2. Create a constructer to assign the input to num.
 * 3. Create a function isDivisibleByThree returning true if divisible by 3 else false
 */
public with sharing class DivideOperation {
    public DivideOperation() {

    }
}